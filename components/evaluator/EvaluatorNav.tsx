/**
 * EvaluatorNav is deprecated — navigation is now handled by the sidebar in the evaluator layout.
 * This component is kept as a no-op for backwards compatibility with pages that import it.
 */
export function EvaluatorNav() {
  return null;
}
